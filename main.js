
var pgre = Object.create({
    
    vocab : null,

    init : function() {
        
    },

    loadVocab : function() {
        return this  
    },

    match : function(text, domElem) {
        text = text.toLowerCase();
    }
         
    
}};
